name: Build iOS IPA

on:
  workflow_dispatch:
    inputs:
      edition:
        description: 'Build edition'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - zhenguojian (all sources)
          - hongguojian (default)
  push:
    branches: [main, master]
    paths:
      - 'lib/**'
      - 'ios/**'
      - 'native/**'
      - 'scripts/build_ios.py'
      - '.github/workflows/build-ios.yml'

permissions:
  contents: read

concurrency:
  group: ios-ipa-${{ github.ref }}
  cancel-in-progress: true

env:
  GOPROXY: https://proxy.golang.org,direct
  GOSUMDB: sum.golang.org

jobs:
  build-ios:
    runs-on: macos-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - edition: zhenguojian
            arguments: '--all-sources'
          - edition: hongguojian
            arguments: ''
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/setup-go@v5
        with:
          go-version-file: native/go.mod
          cache-dependency-path: native/go.sum

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.4'
          channel: stable
          cache: true

      - name: Build iOS unsigned package & IPA
        run: python3 scripts/build_ios.py ${{ matrix.arguments }}

      - name: Upload iOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.edition }}-ios-unsigned-ipa
          path: dist/ios/*
          if-no-files-found: error
          retention-days: 14
